package com.github.intrigus.ftd.block;

/**
 * argument_reporter_boolean and argument_reporter_string_number currently have
 * exactly the same functionality. They exist as classes, because
 * deserialization requires every opcode to have a matching class.
 */
public class argument_reporter_boolean extends argument_reporter_base {
}